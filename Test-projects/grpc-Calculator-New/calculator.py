import math


def square_root(x: float) -> float:
    y = math.sqrt(x)
    return y


def absolute_value(x: float) -> float:
    return abs(x)


def sum(x: float, y: float) -> float:
    return x + y
